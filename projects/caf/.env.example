# Configuración del proyecto CAF — SQL Server
# Copia este fichero a .env y rellena los valores reales.
# ¡NUNCA subas el fichero .env a git!

# Motor de base de datos (sqlserver | oracle)
DB_ENGINE=sqlserver

# Servidor SQL Server
DB_SERVER=tu_servidor.database.windows.net

# Nombre de la base de datos
DB_DATABASE=nombre_de_tu_bd

# Credenciales
DB_USERNAME=tu_usuario
DB_PASSWORD=tu_contraseña

# (Opcional) Driver ODBC. Por defecto: {ODBC Driver 17 for SQL Server}
# DB_DRIVER={ODBC Driver 13 for SQL Server}
